agent_name = "pipecat-moq-example"
secret_set = "pipecat-moq-example-secrets"
agent_profile = "agent-1x"

[scaling]
	min_agents = 1

[build]
context_dir = "."
dockerfile = "Dockerfile"

[build.exclude]
patterns = [".venv/", ".ruff_cache/", ".pytest_cache/", ".env", "tests/"]
